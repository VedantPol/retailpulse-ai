# Optional. The app runs without these keys using a deterministic rule-based insight generator.
OPENAI_API_KEY=
GEMINI_API_KEY=

# Local artifact path. Docker Compose overrides this to /app/artifacts inside the backend container.
ARTIFACTS_DIR=backend/artifacts
DATA_SEED=42

